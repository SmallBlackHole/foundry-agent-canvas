# Dependencies — install with `npm install`, never commit.
node_modules/

# Local UI selection persistence (subscription/project picker) — machine-specific,
# never commit.
.selection.json

# Local env / secrets
.env

# Build output — produced by `npm run package` in CI, published to GitHub
# Releases, never committed.
dist/
